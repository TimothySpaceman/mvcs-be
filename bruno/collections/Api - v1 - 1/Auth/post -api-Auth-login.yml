info:
  name: post /api/Auth/login
  type: http
  seq: 2
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/api/Auth/login"
  body:
    type: json
    data: |-
      {
        "emailOrUsername": "",
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/Auth/login"
      method: POST
      body:
        type: json
        data: |-
          {
            "emailOrUsername": "",
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
