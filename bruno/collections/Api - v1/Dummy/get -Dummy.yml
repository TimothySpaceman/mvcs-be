info:
  name: get /Dummy
  type: http
  seq: 1
  tags:
    - Dummy

http:
  method: GET
  url: "{{baseUrl}}/Dummy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/Dummy"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
