info:
  name: get /Dummy/connections
  type: http
  seq: 2
  tags:
    - Dummy

http:
  method: GET
  url: "{{baseUrl}}/Dummy/connections"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/Dummy/connections"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
